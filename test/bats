#!/usr/bin/env bats

@test "Check zero arg checks" {
	run ./src/app-monitoring
[ "${status}" -eq 1 ]
}

@test "A test I don't want to execute for now" {
	run ./src/app-monitoring.sh https://google.com  abcd
	 [ "${status}" -eq 0 ]
}

@test "test for equality" {
  A=1
  B=1
  [ "$A" -eq "$B" ]
}